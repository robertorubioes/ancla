includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 6

    paths:
        - app
        - config
        - database
        - routes
        - tests

    ignoreErrors:
        - identifier: missingType.generics
